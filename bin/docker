#!/bin/bash

# this script is starting docker and execute docker with the given args

SERVICE='docker.service'
EXECUTE='/usr/bin/docker'

ARGS="$@"

# temporarly disabled because I'm currently 
# not using docker as before
# ~/bin/start-service $SERVICE

$EXECUTE $ARGS
